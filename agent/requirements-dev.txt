# Dev/CI-only dependencies for the concierge agent. Installed in CI (and by contributors running the
# test layer) but NOT baked into the production runtime image — the Dockerfile installs requirements.txt
# only, so pytest never ships to the deployed agent. Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<10
